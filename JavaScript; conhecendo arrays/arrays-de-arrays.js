const alunos = ['João', 'Caio', 'Julio', 'Bruno'];

const media = [10, 9, 8, 3, 5];

const lista = [alunos, media];

console.log (
    `a aluna na posição 1 é ${lista[0][1]}
    e a sua nota é ${lista[1][1]} `
)